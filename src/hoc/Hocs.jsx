import React from "react";

// un HOC es una función que recibe un componente y devuelve un nuevo componente.

// los HOCs no son parte de la API de React, son una convención que se usa para reutilizar lógica de estado y comportamiento entre componentes.

/* 
    const Hoc = (Componente) => {
        const props = {}
        return <Componente {...props} />
    }
*/

const Hocs = () => {
  return <div>Hocs</div>;
};

export default Hocs;
